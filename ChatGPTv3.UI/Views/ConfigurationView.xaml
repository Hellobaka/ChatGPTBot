<UserControl
    x:Class="ChatGPTv3.UI.Views.ConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ChatGPTv3.UI.ViewModels"
    d:DesignHeight="1200"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  ── TabControl ──  -->
        <TabControl
            x:Name="ConfigTabs"
            Grid.Row="0"
            Background="Transparent"
            BorderThickness="0"
            ItemsSource="{Binding Tabs}"
            TabStripPlacement="Top">

            <TabControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ConfigTab}">
                    <TextBlock
                        Padding="12,6"
                        FontSize="13"
                        Text="{Binding Name}" />
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate DataType="{x:Type vm:ConfigTab}">
                    <ScrollViewer>
                        <StackPanel Margin="4,8,16,8">
                            <ItemsControl Focusable="False" ItemsSource="{Binding Sections}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:ConfigSection}">
                                        <Border
                                            Margin="0,0,0,16"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                            <StackPanel>
                                                <!--  Section header  -->
                                                <Border
                                                    Padding="14,10"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="0,0,0,1">
                                                    <TextBlock
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                        Text="{Binding Title}" />
                                                </Border>
                                                <!--  Section items  -->
                                                <ItemsControl
                                                    Margin="14,8"
                                                    Focusable="False"
                                                    ItemsSource="{Binding Items}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="{x:Type vm:ConfigEntry}">
                                                            <ContentControl Loaded="OnConfigRowLoaded" />
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <!--  ── Bottom bar ──  -->
        <Border
            Grid.Row="1"
            Padding="16,10"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal">
                <Button
                    MinWidth="100"
                    Margin="0,0,12,0"
                    Command="{Binding SaveAllCommand}"
                    Content="保存"
                    Style="{StaticResource ButtonPrimary}" />
                <Button Command="{Binding ResetAllCommand}" Content="重置为默认值" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>