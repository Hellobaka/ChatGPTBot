<hc:Window
    x:Class="ChatGPTv3.UI.Views.KeyEditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    Title="{Binding WindowTitle}"
    Width="980"
    Height="620"
    BorderBrush="{DynamicResource BorderBrush}"
    Style="{StaticResource WindowWin10}"
    WindowStartupLocation="CenterOwner">

    <Border
        Background="{DynamicResource PopupBackgroundBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid Margin="16,8,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>


            <!--  Content  -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!--  Provider fields  -->
                    <WrapPanel Margin="0,0,0,10">
                        <StackPanel Width="300" Margin="0,0,12,10">
                            <TextBlock FontWeight="SemiBold" Text="名称" />
                            <TextBox Margin="0,6,0,0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Width="300" Margin="0,0,12,10">
                            <TextBlock FontWeight="SemiBold" Text="EndPoint" />
                            <TextBox Margin="0,6,0,0" Text="{Binding EndPoint, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Width="300" Margin="0,0,12,10">
                            <TextBlock FontWeight="SemiBold" Text="API Key" />
                            <TextBox Margin="0,6,0,0" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </WrapPanel>

                    <CheckBox
                        Margin="0,0,0,12"
                        Content="腾讯云签名"
                        IsChecked="{Binding UseTencentSign}" />

                    <!--  Model grid  -->
                    <TextBlock
                        Margin="0,0,0,8"
                        FontSize="14"
                        FontWeight="Bold"
                        Text="模型配置" />
                    <WrapPanel Margin="0,0,0,8">
                        <Button
                            Margin="0,0,8,0"
                            Command="{Binding AddModelCommand}"
                            Content="新增模型"
                            Style="{StaticResource ButtonPrimary}" />
                        <Button
                            Command="{Binding RemoveModelCommand}"
                            CommandParameter="{Binding SelectedModel}"
                            Content="删除选中" />
                    </WrapPanel>
                    <DataGrid
                        Height="200"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        ItemsSource="{Binding Models}"
                        SelectedItem="{Binding SelectedModel}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn
                                Width="55"
                                Binding="{Binding Enabled}"
                                Header="启用" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                Header="模型名" />
                            <DataGridTemplateColumn Width="90" Header="输入/M">
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding InputPricePer1M, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding InputPricePer1M, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="100" Header="缓存/M">
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding CachePricePer1M, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CachePricePer1M, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="90" Header="输出/M">
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding OutputPricePer1M, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OutputPricePer1M, StringFormat=F4}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="70" Header="测试">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button
                                            Padding="6,2"
                                            Command="{Binding DataContext.TestModelCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Content="测试"
                                            Style="{StaticResource ButtonPrimary}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ScrollViewer>

            <!--  Bottom buttons  -->
            <WrapPanel
                Grid.Row="2"
                Margin="0,16,0,0"
                HorizontalAlignment="Right">
                <Button
                    MinWidth="100"
                    Margin="0,0,12,0"
                    Command="{Binding SaveCommand}"
                    Content="保存"
                    Style="{StaticResource ButtonPrimary}" />
                <Button
                    MinWidth="100"
                    Command="{Binding CancelCommand}"
                    Content="取消" />
            </WrapPanel>
        </Grid>
    </Border>
</hc:Window>
