<UserControl
    x:Class="me.cqp.luohuaming.ChatGPT.UI.Controls.EditableListBox_String"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:me.cqp.luohuaming.ChatGPT.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ListBox
            x:Name="EditableListBox"
            MinHeight="70"
            Margin="0,5,0,0"
            SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="EditableListBoxRemoveButton"
                Grid.Column="0"
                Width="50"
                Margin="0,0,10,0"
                Click="EditableListBoxRemoveButton_Click">
                删除
            </Button>
            <TextBox
                x:Name="EditableListBoxAdd"
                Grid.Column="1"
                PreviewKeyDown="EditableListBoxAdd_PreviewKeyDown"
                Text="{Binding AddItemInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Button
                x:Name="EditableListBoxAddButton"
                Grid.Column="2"
                Width="50"
                Margin="10,0,0,0"
                Click="EditableListBoxAddButton_Click">
                添加
            </Button>
        </Grid>
    </Grid>
</UserControl>
